export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-3xl flex-col items-start justify-center gap-4 px-6">
      <h1 className="text-3xl font-semibold tracking-tight">Reelcue</h1>
      <p className="text-neutral-600">
        Video review and approval, without the overhead. Replace this page
        with the real marketing site once the core flows are working.
      </p>
    </main>
  );
}
